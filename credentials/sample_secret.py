# Use the Python `os` or `secret` module to generate a random secret key and store it to the `key`
# secret_key = os.urandom(32).hex()
# secret_key = secrets.token_hex(32)
# Copy and/or rename file, removing "sample_" prefix
key = ""